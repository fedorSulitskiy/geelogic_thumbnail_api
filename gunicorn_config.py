# Number of worker processes
workers = 4  
# Host and port to bind to
bind = "0.0.0.0:8000"  
# Request timeout in seconds
timeout = 120 

# Use the console as the error log destination
errorlog = '-'
# Use the console as the access log destination
accesslog = '-'
# Set the log level (options: debug, info, warning, error, critical)
loglevel = 'info'